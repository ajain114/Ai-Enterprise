# requirements-dev.txt
# Development, testing, and linting dependencies

-r requirements.txt

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
pytest-mock>=3.12.0

# ── Code Quality ─────────────────────────────────────────────────────────────
ruff>=0.3.0                       # Linting + formatting (replaces flake8, black, isort)
mypy>=1.8.0                       # Static type checking
pre-commit>=3.6.0                 # Pre-commit hooks

# ── Documentation ─────────────────────────────────────────────────────────────
mkdocs>=1.5.0
mkdocs-material>=9.5.0
